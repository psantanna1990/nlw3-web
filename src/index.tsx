import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Primeiro arquivo carrregado dentro da minha aplicação

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
